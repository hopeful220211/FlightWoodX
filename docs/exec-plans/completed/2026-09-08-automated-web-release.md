# 前端自动发布

> 状态：已完成本轮受限前端发布、实际权限与受控回退恢复验收；未进行正式故障注入
>
> 更新时间：2026-09-08
>
> 适用范围：GitHub 托管 CI 到现有 ECS 的前端发布
>
> 替代关系：补充发布操作记录，不替代 CURRENT_STATUS.md 或后端发布流程

## 目标和边界

用户批准建立一次授权、以后无需反复登录 Workbench 的发布流程。以 `3107c42` 为起点，隔离分支 `codex/automated-web-release-2026-09-08` 实施；首次发布包括此前 G-Mark 更正。不改原工作树、默认分支、API、数据库、上传卷、证书或现有管理员认证。

正式发布分支采用独立 `production`，由通过 CI 的当前开发提交建立；生产环境 `ecs-production` 只允许该分支，禁止任意开发分支或 PR 使用部署凭据。CI 仍在 GitHub 托管机器执行，正式服务器不安装通用 Actions runner。

## 接口与安全

- 在真实浏览器测试成功后，打包该次确切 SHA 的公开 dist；包含 `release.json`，记录 SHA、全部文件大小和 SHA-256。
- 专用用户 `fwx-deploy` 使用 SSH 强制命令，只接受 `status`、`publish <SHA> <archive-sha256>`、`rollback <SHA>`。发布压缩包通过标准输入传输；回退只允许当前 SHA 的上一版本。
- 密钥存 GitHub 生产环境 Secrets；主机密钥必须与已信任 known_hosts 或服务器人工读取的公钥一致，禁止关闭主机验证。不开放 Docker 组、任意 sudo、交互 shell 或转发。
- root 拥有的固定发布器与配置不从上传包更新。拒绝目录穿越、链接、特殊文件、重复路径和隐藏凭据；限制压缩 256 MiB、解压 512 MiB、10000 条目及接收时间。只在全新 root-owned 目录解包，不覆盖 live dist。
- 固定 Compose 快照只允许切换 nginx；API/Mongo 身份必须保持。配置、公开入口/资源和 API 检查失败时回退，串行锁避免并发切换。保留旧目录，禁止自动删库或清理用户数据。

## 顺序与验收

1. 测试先行实现归档、受限 SSH、服务器发布和恢复逻辑；本地隔离测试成功后独立安全复核。
2. 完整 Harness/CI，核对 GitHub 当前 SHA 全部作业结果；准备哈希固定的首次安装指令。
3. 通过 GitHub API 配置专用生产环境、分支约束和部署凭据。首次服务器初始化必须由用户在 Workbench 执行；当前浏览器工具禁止该网址，不能绕过。
4. 真实 SSH 核验身份限制、拒绝任意命令、状态和已信任主机密钥；网络不通或身份不符则停止，不盲目放宽防火墙。
5. 首次自动发布后核验线上 SHA/摘要、前三奖项/数量/图片、三个视口与 API 健康，验证受控回退、恢复及服务端 API/Mongo 身份检查；故障自动补偿以隔离测试覆盖。
6. 更新当前状态、部署说明与接续入口。只有真实服务器与公网站点验证完成才把计划标为完成。

## 实施期间证据（历史）

- 基线 `3107c42`：本地 446 项及 GitHub 8/8（含浏览器 4/4）通过；奖项仍未发布。
- 初始化前本机没有可用 SSH 私钥或 agent 身份。此次已生成专用 Ed25519 密钥，私钥仅留受限本机文件和 GitHub 环境 Secret；仓库只包含公钥。主机 `8.156.92.182` 的现有可信公钥经真实连接核对一致，TCP 22 可达，尚未安装服务器端授权。
- 新增凭据仅用于此仓库到此网站的前端发布；不使用既有云主账号凭据，不公开密钥。
- GitHub 已建立 `ecs-production`，仅允许 `production`；环境 Secrets `SSH_KEY`、`SSH_KNOWN_HOSTS` 和目标主机/端口变量已回读确认。`production` 当前停在 `3107c42`，要求 8 项检查、线性历史、禁止强推/删除并对管理员生效，尚未推入自动发布工作流。
- 客户端 14 项、服务器 31 项、初始化 12 项测试通过。实际完整 dist → 归档 → 服务器解包器通过：883 个公开文件、918 个解包条目；文件 0644、目录 0755，无隐藏占位。此本地归档绑定基线 `3107c42`，不作为未来提交的正式产物。
- 安全复核修正了 PAX 稀疏文件元数据处理、隐式目录数量限制、公开验证内存占用、事务待恢复状态与未确认回退误报。服务器安装、实际 SSH 权限、首次发布及真实回退仍需后续验收。
- 复核后补同 SHA 重试：最初 9 项新增用例按预期失败；实现后含 13 项新用例的服务器测试通过。重试须完整校验归档、当前文件、挂载、后台身份与 HTTPS，不能重启或重写现有版本。GitHub `Tests` 新增 Python 3.10，官方 setup-python 固定已核对提交；实际兼容结果以后续远端执行为准。
- 最终本机 `pnpm run harness`、`FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run ci` 通过：462 项，API 集成无跳过、依赖审计无已知漏洞、生产构建通过。单独 Python 43 项通过（已含在上述两个根测试包装器，不重复累计）；保留已有 Blockly 大包提示。发布前仍需本次 SHA 远端 8 项通过。

## 正式执行结果（2026-09-08）

1. `77e4a194ad81d711479df86d39f7e3c468963ad3` 的开发分支运行 [34234102951](https://github.com/hopeful220211/flightwoodx/actions/runs/34234102951) 8/8 成功；实际使用 Python 3.10.21。生产分支仍在 `3107c42` 时独立回读了保护、唯一环境分支策略、Secrets 名称和目标变量，没有读取或输出密钥值。
2. 用户首次安装被旧 ACME 路径的 UID 501 所有权拦截，安全检查未放宽。诊断只发现 `/root/flightwoodx`、其 `deploy`、`deploy/nginx` 和 `deploy/nginx/certbot-www` 四个目录需要调整。用户先备份数值 UID/GID/权限/设备/inode 到 `/root/fwx-ownership-before-jhyjdl`，再仅对四个目录非递归改为 root，保留 0755 和全部内容。固定 SHA 的三文件摘要通过后安装成功；没有切换旧网站或改动证书、数据库。
3. 部署公钥指纹 `SHA256:bK1W8gJYE73aDmZLP0mXEXK/fSXR8DwdLslpZXcyEdM`，主机指纹 `SHA256:ieH7JtVrxShN01pZX0e+5lnz/pp6sddJNzmznuI4TFg`，均与既有可信记录一致。真实 SSH `status` 返回 `ready:true`、初始两 SHA 均 null。只读任意命令 `id` 被拒（exit 1，无 uid 输出），PTY 与远程端口转发均明确拒绝（exit 255）。没有开放通用 shell。
4. 普通快进 `production` 至 `77e4a19`，无强推或保护绕过。正式运行 [34237204343](https://github.com/hopeful220211/flightwoodx/actions/runs/34237204343) 的 8 个检查及 `Publish tested frontend` 全部成功，北京时间 22:21:17 返回当前 SHA 和 `ready:true`。浏览器 4/4，1.7 分钟：桌面 21.7 秒、手机 25.3 秒、188 个资源 0.89 秒、自制混合件 49.1 秒。
5. 使用该正式运行的产物 `10060519837`：ZIP 142,885,646 字节，摘要 `a94b9e90730f2257ab686c5e7a594d6d621cf5e09fa0b0a5ebc0b9498a2c71f0`；内层 tar.gz 142,885,335 字节，摘要 `b4bdc24691803e568a58213f24ab12a308d3fd70a8a4af287a737dce69b78129`。下载后重新计算归档摘要，归档中清单与正式 `release.json` 完全一致：883 个公开文件，共 162,669,350 字节。
6. 独立客户端核对正式版本、首页/入口 SHA-256 和 API 健康。正式 Chrome 的 390×844、768×1024、1440×900 全部通过，实际查看截图：仅 Red Dot/iF Design/IDEA，相关数量为三，图片读取正常、按钮滚动正确、无溢出、错误、警告或失败请求。Browser plugin not available，使用独立 Playwright Chrome；没有复用用户登录状态或创建正式测试数据。
7. 等原发布作业整体成功后执行 `rollback 77e4a19…`。状态变为 `current=null / previous=77e4a19 / ready=true`；公网旧首页摘要 `0668366f192bcfeba2a82ff5760b4b6aec996c4018578cb62910c38814b053c8`、旧入口 `index-D9E4hbkC.js` 摘要 `89aed05c36572df9dd63c316c8f16b2217c13d421a327a9d8aa1309315a9efec` 均与回退前记录一致，API 仍 `OK / connected`。null 表示原目录没有版本清单，不表示目录丢失。
8. 随后通过同一受限客户端重新发布第 5 项的测试归档，截至 22:26:27 已确认恢复 `current=77e4a19 / previous=null / ready=true` 并通过独立公网摘要与 API 检查。新首页摘要 `45e682ef34c0f6492d2ed8b42200d992826b85a17b3258b888bea4e86772206d`，入口 `index-VGPvOoS_.js` 摘要 `e8626c71aa8b530297f7d6a0d3527698dbc1b7b3f203728102e5d9d083258d30`。每次切换均由已审查的服务器端程序核对 API/Mongo 身份和 live 挂载；客户端没有另行读取容器完整配置或取得通用命令权限。

## 交付与剩余边界

- 正式前端保留 `77e4a19`；收尾文档提交在开发分支另行推送，不为文档再次切换 nginx。原始脏工作树、main、旧网页目录及已有用户数据未清理。
- 恢复新版后，正式 Chrome 三个视口再次全部通过；奖项图片、数量、按钮滚动、布局、页面/控制台和请求检查均无失败。此回归不写入账号或业务数据。
- 日常前端更新已可由 GitHub 发布，不再依赖 Workbench 登录。另一台电脑不用复制私钥，按当前状态接续代码和文档；密钥轮换、主机身份变化、空间不足和恢复失败仍需维护。
- 受控回退与恢复已实测；故障自动补偿和同 SHA 重试的错误分支有隔离测试，没有进行正式站故障注入或将其表述为实测。nginx 会重建，可能短暂断开连接。
- 此流程不自动升级 API、Mongo、证书、配置或部署器。正式管理员、社区写入、自制绘图完整业务验收及硬件/制造证据仍以 `CURRENT_STATUS.md` 登记为准，不因自动发布完成而关闭。
