# 评审前现有网站修复

> 状态：进行中
>
> 更新时间：2026-09-07
>
> 适用范围：现有官网、鉴权、零件、拼装、积木编程、仿真、作品保存及已有管理入口的浏览器验收与修复
>
> 替代关系：记录本轮执行，不替代当前架构、产品规格或历史验证结果

## 目标与范围

用户要求两天后评审前让官网现有功能实际可用，修复图片、拼装、登录注册、保存与积木编程，并规范界面；不新增业务模块。保留原有主要页面文案。软件规则和视觉仿真不作为实飞或制造证明。

## 基线与事实

- 原工作区 `chore/add-speckit` 有无关未提交工作，保留原状。
- 从已上传的 `934c183` 建立隔离分支 `codex/review-readiness-2026-09-07`。
- 线上首页返回的 Last-Modified 为 2026-07-03，入口为 `index-CYcC3otM.js`；阿里云 ECS/nginx 在提供正式网站，GitHub 的交付分支不等于该站已经发布。
- 线上 OSS 模型、缩略图、纹理均返回 403 AccessDenied；现有官网图片约 101 MB。`/api/health` 返回数据库已连接，但不能证明具体业务成功。
- 本机 SSH 别名 `fwxecs` 的 IdentityFile 指向已消失的临时文件，连接返回 Permission denied(publickey)。2026-09-07 用户登录后，已通过现有 ECS Workbench 免密入口进入 root 会话，访问阻断解除；没有读取/上传私钥正文或更改登录凭据。
- 上轮 235 项测试只证明当时工程基线；本轮重新验证真实浏览器与服务。

## 顺序与分工

1. 调查线上部署、失败资源、API 和访问条件。
2. 在隔离开发环境复现，分别修复拼装、编程仿真、鉴权持久化及页面状态。
3. 检查首页及已有页面图片、导航、键盘焦点、加载/空/错误状态和响应式布局。
4. 浏览器连续验证注册登录、零件、拼装、编程、仿真、保存、刷新再打开、导出和已有管理权限。
5. 运行目标测试、Harness 和完整 CI，独立审查修复。
6. 在可确认目标与恢复方式的部署环境发布，回读线上版本并重跑关键路径；缺少凭据则明确报告并请求所需访问。

## 验收与恢复

覆盖 390×844、768×1024、1440×900，记录控制台与失败请求。测试使用专用账户和隔离数据；不删除已有用户数据，不执行破坏性迁移。部署前确认当前版本、备份及回滚路径。截图、临时脚本和日志保存在仓库外；重要结论写回此计划与 CURRENT_STATUS。

## 实际结果

### 已修复

- 官方资产保留同源副本；35 张网页图片生成 WebP，101.1 MB → 3.8 MB，原图仍保留；Google 字体请求移除，Blockly 素材随构建复制到同源目录并附许可证。
- 拼装 ID、步骤推进、删除父件残留子件、双主板落点、翻转父子变换、拖拽/触控一致性、镜头、错误重试和手机布局。
- 积木清空/无效 XML 草稿保留、服务端失败不重建覆盖、作品程序绑定、仿真停止/重置/重新运行、实际拼装模型显示及编辑模式导航。
- 真实慢速浏览器复现定时器迟到导致仿真按比例拖长，改用单调时间推进、完整路径碰撞检查和真实 10 秒条件等待；保留停止取消。相同 CPU 降速 12 倍条件下，约 10 秒程序从 18.0 秒恢复至 10.1 秒，不靠放宽浏览器断言。
- 会话响应格式、用户资料保存、跨账号隔离、过期后重新登录；作品保存统一按账号与作品串行，切页刷新、同账号令牌变化、删除后不被旧保存复活均补测试。
- 公开作品撤回后的社区、榜单、评论、收藏、关注和旧兼容入口可见性；非法输入返回 400/409，不泄露数据库堆栈。
- 已有管理员用户 API 接入只读列表、搜索、角色筛选和分页；保留双重权限校验，不新增权限修改能力。
- 持续编辑请求按验签账号 600 次/15 分钟计数，未认证保留 100/IP，登录/注册/改密码保留 20/IP。签名仅用于配额键，不替代用户/令牌版本校验；不声称分布式限流。
- nginx 静态资源不再回退 HTML，新增上传反代和持久化卷、可信代理设置；部署脚本补资产探测和 API 健康检查。后续正式机器执行记录见文末。
- 修补当前依赖审计报告的 sharp、browserslist、qs、fflate、postcss-selector-parser 漏洞，未关闭检查或删除断言。

### 已验证与最终门槛

- 隔离真实 MongoDB 7.0.24，不使用生产数据；API 覆盖身份、角色、作品、绘制零件、程序、社区、赛事、上传、撤回和改密码。
- 实际 Chrome 完成画闭合轮廓→立体预览→保存→刷新；注册/重新登录、6 件官方拼装、Blockly 示例/清空/恢复、仿真运行停止重置完成；社区发布、点赞、评论、收藏保存刷新。
- 管理员真实登录→访问密钥验证→用户列表→教师筛选→空搜索→刷新；学生持有效密钥仍被 403 拒绝。
- 首页、拼装、编程、个人资料和管理员列表检查 390×844、768×1024、1440×900；修正平板首页 4 px 溢出，最终检查无横向溢出。
- 浏览器测试已进入 `apps/web/e2e/core-flow.spec.ts`，默认禁止生产地址和外部网络；生产构建完整重跑结果以 `CURRENT_STATUS.md` 为准。
- CI 增加真实 MongoDB 服务和独立 Browser core flow；本机完整检查与远端结果分别记录，不能相互替代。
- 本机生产浏览器回归增加全部 94 项零件、188 个模型/缩略图的同源 HTTP 与二进制核验，3/3 通过（41.7 秒）。首轮远端工程 6 项通过，但桌面仿真超时；保留失败记录并继续定位，不放宽完成断言。
- Node/CI/Docker 统一到 Node 22（最低 22.12.0）；新增独立生产 API 容器加临时 MongoDB 作业。本机无 Docker，配置与脚本检查通过不等于镜像运行通过。
- `a168721` 的远端 CI 8/8 通过（运行 `34048490808`）：容器生产镜像实际构建、启动、数据库健康检查成功；浏览器 3/3。随后计时修复的本机完整 CI 324/324 通过，远端以最后提交的 Checks 为准。
- 计时修复后最终生产构建浏览器 3/3（40.2 秒），零外网/HTTP 失败/页面异常；独立审查与 10 个额外确定性碰撞/停止边界检查通过。原有完成断言、超时与重试数不变。
- 计时修复提交 `b456ab6` 的远端运行 `34048765595`：8/8 作业通过，浏览器 3/3（54.1 秒），API 生产容器实际启动并连接 MongoDB。
- 发布说明独立复核修正首次证书顺序、根目录命令、证书具体文件路径、宿主机 Node/pnpm 前提和数据库备份范围；装机脚本提示不再要求删除证书目录。仅说明与提示调整，未操作服务器或证书。
- 发布说明提交 `5c0303a` 的远端运行 `34049026354`：8/8 作业通过。本次后续仅更新权限和运维核验记录。

### 切换前的正式服务器访问核验

- 用户已授权代理负责数据库、服务器调试和发布，用户负责验收。通过浏览器已登录控制台与现有 Workbench 免密入口，`id` 实测 root；未新建凭据、修改权限或关闭安全限制。
- nginx、API、MongoDB 三个正式容器运行，API 内部健康接口 200 且数据库 connected，MongoDB ping 为 1。`mongodump` 100.17.0 可执行；未执行备份或恢复。MongoDB 无宿主机公开端口。
- 生产 API 为 Node 20.20.2、OSS 驱动；宿主机无 Node，部署目录不是 Git 仓库。发布不能直接执行 Git 更新或假设本地构建工具齐全。磁盘约 26 GiB 可用。
- API 没有挂载卷，`/app/apps/api/uploads` 存在 6 个文件、146,928 字节、无符号链接。必须先保护这些文件，再重建 API 或增加上传卷；现有 MongoDB 卷、nginx 前端目录、证书与配置均保留。
- 不输出凭据，仅对固定官方模型使用现有 OSS 凭据执行 HTTPS HEAD，得到 403 AccessDenied。原因未确定，未验证上传，不修改桶公开权限。
- 这次仅只读核验，未写生产数据、部署、重启、备份或变更凭据。后续先建立并核验备份/回滚，再排查存储与发布，最后正式域名业务复测；技术操作不交给用户执行。

### 未关闭

- 正式域名已发布/回读，备份恢复、核心 HTTPS 持久化、244 个资源、模型 403 和 API 重建验收已闭环；剩余正式业务与后补封面/交互修复的验收见当前状态。没有删除生产数据或修改对象存储权限及历史凭据。
- 自制零件已接入混合自由作品和账号恢复；卡扣、历史几何版本、制造检查、获批 DXF、真实飞行证据仍缺失。
- 管理后台课程/零件审核仍为占位，用户和新增审计页仅只读；奖项与学习进度无完整业务来源，不列为已完成功能。
- 多标签页/多设备同时编辑仍是最后写入生效，需要版本冲突协议；旧 Project 兼容层、内容合规、历史凭据、定期异机备份和监控仍待后续完成。

最终执行结果与启动/交付说明见根 `CURRENT_STATUS.md`；上线和恢复步骤见 `deploy/README.md`。本计划在正式网站验收完成前保持 active。

### 后续实际执行（2026-09-07）

- 已备份整站、镜像、配置、证书、6 个上传文件和 MongoDB，归档校验通过；独立无网络容器恢复后，20 个集合计数与生产完全一致。恢复容器已清理，原生产卷不变。
- 原凭据 HTTPS 请求 OSS 固定模型仍为 403，当前登录账号控制台显示尚未开通 OSS；未开通付费服务或公开存储桶。改用同源资产与独立磁盘持久卷，6 个旧文件已逐字节验证复制。
- 新发布目录及 Node 22 容器前后端预构建通过，原站未切换。保留原证书 webroot 并补续期 reload；3 个 localhost 封面引用对应的文件确实存在，补默认只读、精确范围和备份保护的维护脚本。
- 复现跨用户封面清理问题后，所有设计/旧 Project 上传、替换、删除和失败清理均限定服务端资源目录；补 HTTPS 与 SDK 日志脱敏测试。
- 自制件来源契约、自由摆放、混合官方件、新浏览器账号恢复、坐标和导出引用、源变更/删除提示均实测；不添加卡扣、几何版本副本或实飞保证。
- 真实只读审计接入后台；学生学校来自现有资料，不伪造课程/审核数量或账号状态。移除缺依据的飞行重量阈值、默认质量和坐标重心判断。
- 发现旧前端 `tsc --noEmit` 仅检查空的引用入口；改为实际 app/node 两个配置，修复暴露的源码错误并增加防回归检查。历史构建/浏览器结果有效，但旧类型检查不再记为完整源码覆盖。
- `f5b12b3` 本地 CI 387/387、生产构建浏览器 4/4、远端 CI 8/8 通过，已于北京时间 02:33:58 切换 API/nginx；Mongo 未重建。停旧 API 后再次备份、核对 6 文件；3 条封面条件修复，补缺失唯一索引前确认无重复。
- 正式 HTTPS 创建 2 个独立验收账号，私有混合作品、来源零件、程序绑定、封面上传/字节读取、新登录恢复和跨账号拒绝通过；未创建公开测试帖子、未删除既有数据。
- 正式模型请求发现 nginx 403，尚需服务器日志确认权限根因。补生成 dist 专用权限整理及符号链接拒绝，389 项完整 CI 通过；这项尚未部署，不能将模型记作已恢复。
- Chrome 密码自动填充弹层后窗口控制返回空树且无法截图，重建工具连接/重新激活浏览器仍未恢复；其他应用正常、系统未锁屏。已请求用户将已有 Workbench 切到前台；原 SSH 私钥路径不存在，未绕过凭据或控制限制。
- 用户恢复 Workbench 后，nginx 日志和 0600 模型确认权限根因，03:17 左右实际拉取 `a71e822` 并仅整理生成 dist 的 919 个条目；244 项正式资源通过，另验证 5 类缺失资源真实 404。
- 03:20 实际重建 API、检查/reload nginx，Mongo 容器 ID 与创建时间未变；专用测试作品、来源、程序及新封面重新登录回读通过，封面字节相同。
- 正式浏览器新增一个专用测试账号，六件官方拼装保存、积木同步、运行/停止/重置和 10.0 秒完整仿真通过；全新无痕会话重新登录恢复六件、名称和程序。积木三个目标视口、手机社区详情与首页视频实际检查。没有发布公开测试帖子。
- 后补中文命名保护由 6 个失败测试确认并修复；正式浏览器还发现纹理加载时重复警告和缩略图黑影，继续针对图像就绪与抓图时机补测试、修复及最终发布。当前进行中结果不能由此前 CI 推定完成。
- 后补修复代码已冻结：纹理就绪、当前帧抓图、纯自制/混合封面、40mm 模型近裁剪、账号/快照/项目绑定与迟到回调隔离均补回归；封面失败不缓存/不上传，官方 GLB 重试只清当前作品的失败缓存。独立复审发现的缓存问题已先红后绿验证，没有放宽断言。
- 最终本地完整 CI 438/438；之前一轮生产构建浏览器 4/4，包含官方/纯自制/混合封面实际像素、上传 200、同源和逐字节回读。GLB 缓存补丁后的最终浏览器复跑、GitHub Checks 与实际新前端发布分别记录，不能由前一轮推定。
- 用户确认部署完成后会亲自登录网站管理员账号；阿里云 root 登录不作为网站管理员验收证据。
- 缓存补丁后的最终生产构建浏览器再次 4/4（1.2 分钟）；普通及自制流程、封面实际内容与持久化均通过。准备推送本次代码并读取对应远端 Checks，再按独立候选目录方式切换前端；此前线上版本仍保留。
- `e47f0ca` 的远端 CI 8/8、浏览器 4/4 通过，04:00:59（北京时间）仅将 nginx 前端挂载切到独立候选 `/root/flightwoodx-web-e47f0ca`，入口 `index-DqEFyc6i.js`；两项构建摘要与本机一致。API/Mongo 未重建，244 项资源、5 类缺失资源及私有数据重新登录回读再验通过。第一次临时构建因 Node 自动堆上限不足失败，保留失败事实；只增大临时构建进程堆上限后完整重建、整理权限并验证，才切正式入口。
- 上线后实际冷访问发现 `/dashboard` 与同名静态目录冲突（301 后 403）；正在最小修改 nginx SPA 文件回退规则，3 项配置回归先红后绿，不改资源 404、代理或 TLS。将先在仅回环开放的同配置临时 nginx 实测，再只切换正式 nginx，保留前端与全部业务卷。
- 同一浏览器还从缓存加载七月旧入口并因 OSS 失败触发清空本地存储；带版本参数重新请求已读取新页面。源码仍残留该清空逻辑，补充最小修复：异常时保留全部登录、作品/程序草稿与偏好，错误页只提供刷新。3 项 jsdom/React 回归先 2 失败、修复后全部通过，全 Web 196/196；最终构建与实际发布另验，不承诺恢复旧版已经删除的数据。
- `cc23746` 远端 CI 8/8、浏览器 4/4 通过；正式机仅回环临时 nginx 实际验证两个工作台路径 200、HTML 摘要一致、PNG/GLB 文件头、缺失 404 和 API 200。04:13:24 只重建正式 nginx，公开网址 13 项检查通过；API/Mongo 身份未变。
- 异常保留草稿补丁完整本机 CI 444/444，最终构建 `index-D9E4hbkC.js` 的浏览器 4/4（1.2 分钟）。将单独读取对应远端检查并切换候选前端，不把路由发布等同于该补丁上线。
- `7c3fb21` 远端 CI 8/8，浏览器 4/4（1.8 分钟）、真实 API 容器通过。路由修复后的正式 Chrome 验证游客单主板草稿、木色封面和直接刷新恢复，没有新增正式账号或公开帖子；游客本地恢复与此前账号云端恢复分别列示。
- 最终候选在独立目录构建 1 分 13 秒、公开权限 919 条目通过，首页/JS SHA-256 与本机一致；04:21:19 只切换 nginx 前端挂载至 `/root/flightwoodx-web-7c3fb21`。正式首页与两个工作台路径 200/no-cache、JS 摘要一致；244 项资源、5 类缺失 404、API 健康、私有混合作品/来源/程序和封面字节回读通过。API/Mongo 未重建，前一个候选和原 dist 保留。
- 最终 Chrome 强制刷新后游客草稿及木色封面仍正常；仅删除本轮的 `Review Guest Preview` 游客草稿，退出游客模式，将官网停在管理员登录窗口。3 个正式专用账号与私有验收数据保留，未删除已有用户作品。临时 nginx 检查容器已清理，不删卷。
- 用户本人登录管理员及后续正式业务验收仍是待办，因此本计划保持 active；部署完成不等于课程/审核/学生工作流或实飞制造证据已经完成。
