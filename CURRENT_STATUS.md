# FlightWoodX 当前状态

> 状态：工程基线全绿，Harness 基础完成，产品发布阻塞未清
>
> 更新时间：2026-08-17
>
> 验证对象：`codex/harness-engineering` 工作树；工程基线 `f8ab786`；提交后本文描述其所在提交；GitHub 异地交付从相同 tree 建立不含本机开发祖先的快照分支
>
> 适用范围：当前仓库的自动化、模块完成度、人工验收和发布阻塞
>
> 替代关系：覆盖历史审计、路线图、旧 RFC 和已删除的 Web 状态副本对“当前完成度”的描述；不替代 `AGENTS.md` 或 `ARCHITECTURE.md`

本文只记录当前代码和本轮验证能证明的事实。页面、路由、RFC、计划或测试文件存在不代表功能完成。

## 1. 一句话结论

冻结安装、Harness、类型检查、API 语法、235 项测试、lint、当前文件密钥扫描、完整依赖审计和生产构建均通过。本轮没有改页面呈现或文字；Web 的备用 CAD 调用与 API 导出链已收紧：目录归 API 管理，关闭目录穿越，并改为读取登录用户自己的服务端作品与作者，客户端伪造字段不再进入 ZIP。

项目仍不是发布完成版：零件装配规则存在冲突，用户零件尚未完整进入拼装器；没有硬件、制造或实飞证据；真实 MongoDB/对象存储、多设备冲突、Project 迁移、系统级业务、未成年人合规和生产运维尚未闭环。

## 2. 本轮验证

2026-08-17 在 Node `v22.23.0`、pnpm `9.12.0`、Vite `7.3.5`、Vitest `4.1.10` 上重新执行：

| 命令 | 结果 | 能证明什么 |
| --- | --- | --- |
| `pnpm install --frozen-lockfile` | 通过 | lockfile 与全部 workspace 依赖一致 |
| `pnpm run harness` | 通过 | 必需知识入口、元数据与链接、Skill 单一来源、静态架构边界和遗留文件禁令满足当前规则 |
| `pnpm run ci` | 通过 | Harness、typecheck、API syntax、test、lint、security 和 build 全部执行并通过 |
| `pnpm test`（由 CI 执行） | 通过，235 项 | Harness 101；应用测试 134：shared 25、geometry 16、parts-schema 8、flight-check 6、web 49、API 30 |
| `pnpm audit` | 通过，0 个已知漏洞 | 当前 lockfile 的完整依赖树未报告已知漏洞 |
| `pnpm audit --prod` | 通过，0 个已知漏洞 | 当前生产依赖树未报告已知漏洞 |
| Skill `quick_validate.py` | 通过 | `flightwoodx-development` 的结构与元数据合法 |

CI 已把 Repository harness 作为独立门禁，并继续执行类型/API 语法、测试、lint、安全和生产构建；本轮把 `codex/**` 加入 push 触发，使异地交付分支也运行远端 CI。Harness 新规则采用测试先行：最终 101/101 通过；JavaScript/TypeScript 源码引用使用 TypeScript compiler AST 解析，TypeScript/JavaScript 配置使用当前工具链解析，样式与 Vite HTML 入口使用有范围的静态扫描。它不是完整的跨程序文件系统、数据流、Vite 插件或 CSS/HTML 语义分析；Markdown 与 YAML 也仍是有范围的机械检查。

安全依赖已更新：`js-yaml 4.3.1`、`nanoid 3.3.18`、`@aws-sdk/client-s3 3.1111.0`、`@ungap/structured-clone 1.3.1`。安装仍报告 `whatwg-encoding 3.1.1` 为上游弃用依赖，但当前审计未把它列为漏洞。

生产构建通过，现有体积未因本轮变化改善：启动 `index` 369.82 kB（gzip 113.55 kB），CSS 546.89 kB（gzip 226.36 kB），`CodingPage` 724.48 kB（gzip 192.34 kB）仍触发大于 500 kB 的提示。

本轮没有改页面呈现或文字；Web 的备用 CAD 请求已随服务端授权边界调整，但没有执行该路径的新浏览器验收。固定多视口 E2E、无障碍、真实数据库/对象存储和目标环境仍未验证。

## 3. Harness 与清理结果

| 范围 | 当前状态 | 结果 |
| --- | --- | --- |
| 规则入口 | 完成 | 根 `AGENTS.md` 压缩为地图；Web、API、packages 使用就近规则 |
| 知识结构 | 完成 | 建立 `docs/index.md`、核心流程规格、质量、安全、可靠性、债务和执行计划入口 |
| 项目 Skill | 完成 | 仅保留 `.agents/skills/flightwoodx-development`，并通过官方校验 |
| 自动约束 | 完成 | 101 项 Harness 测试接入本地 `check` 与 GitHub CI；覆盖清单依赖、入口与别名，直接源码引用，常用 Node 文件路径，Vite glob/配置，样式与 HTML 本地入口，以及非生成目录中的仓库符号链接 |
| 无关 Skill | 完成 | 删除 27 个 Lark 实体目录、两组共 54 个遗留链接、Feishu 锁文件和旧 `fwx-motion` Skill |
| 权限残留 | 完成 | 删除 3 份会放宽 push/remote/restore 等权限的 `settings.local.json`，并加入忽略规则 |
| 过时文档 | 完成 | 删除冲突的 Web 状态、Admin/API/环境旧教程；Vite 模板和资源说明改为当前入口与证据边界 |
| 依赖安全 | 完成 | 两个新出现的高危审计项已升级关闭；完整与生产审计均为 0 |

这些结论只适用于本隔离工作树和其交付提交。原始脏工作树没有被改写；合并前其文件和当前任务已缓存的 Skill 列表不会自动变化。

## 4. 模块进度

| 模块 | 当前进度 | 主要缺口 |
| --- | --- | --- |
| 零件绘制/选择 | 部分可用 | 用户零件有共享契约和服务端 SVG 复核，但尚未通过统一输入完整进入拼装器；制造检查与真实切割未完成 |
| 无人机拼装 | 官方零件主路径可用 | 步骤文案允许 4–8 个起落架，`flight-check` 默认要求恰好 4 个，规则未冻结前不能继续复制 |
| Blockly 与视觉仿真 | 作品级主路径完成 | Program 已绑定作品且无静默 DEMO；`RunResult` 仍是客户端临时结果，不是物理仿真或实飞证据 |
| 作品保存与管理 | 主路径完成 | 服务端 `DroneDesign` 与幂等保存已建立；多设备冲突、Mongo `Mixed` 和 Project 兼容迁移未闭环 |
| 部分导出 | 软件路径可用 | 已有合法轮廓可生成 DXF/SVG；备用端点只读取当前用户的服务端作品和作者，但目录没有获批 DXF，官方零件也缺获批二维制造源，不能称完整加工包 |
| 鉴权与 API | 基础线完成 | 真实 MongoDB/对象存储、并发约束和生产会话未验证；部分路由仍内嵌大量业务与数据访问 |
| 社区、赛事、成员/学生管理 | 路由与部分基础测试存在 | 缺系统级 API/E2E、完整状态机、越权和未成年人合规验收 |
| 部署与运维 | 构建与骨架存在 | 缺目标环境回读、结构化日志、指标、告警、备份和恢复演练 |

## 5. 当前 P0 阻塞

1. **冻结唯一装配规则。** `parts-schema` 的 4–8 个起落架与 `flight-check` 的恰好 4 个互相冲突；需负责人结合产品和硬件依据确定，再由共享来源和同一组 fixture 统一前后端。
2. **冻结内容证据或降级声明。** 首页等现有文字按要求未改，其中飞行、奖项、案例和供应承诺仍缺来源；在提供证据或授权降级前不能作为发布事实。
3. **建立硬件与制造证据。** 缺获批二维制造源、材料/公差/切割参数、完整硬件实测和实飞记录。
4. **完成真实外部服务验证。** 需验证 MongoDB、唯一索引/事务并发、OSS/S3、STS、失败恢复和生产会话方案。
5. **闭环作品同步与迁移。** 定义多设备冲突处理，完成 DroneDesign 单一来源迁移并逐步退出 Project 作品语义。
6. **补系统级业务与合规验收。** 覆盖社区、赛事、成员/学生管理的权限、状态机、E2E 与未成年人保护。
7. **补生产运维闭环。** 完成可观测性、告警、备份、恢复演练和上线回读。
8. **关闭公开仓库与历史凭据风险。** GitHub 仓库当前已经公开，但历史凭据轮换、受控历史清理和 GitHub 密钥扫描/推送保护尚未完成；仓库中的儿童相关图片也缺肖像与使用授权核验。优先转为私有并完成轮换、清理、授权和远端复核；本轮以无父根快照建立独立交付历史，避免新增公开 37 个本机祖先提交。

## 6. 下一步主功能

下一轮先处理“零件 → 拼装”这一段，不同时扩张其他模块：

1. 由负责人冻结起落架唯一规则和证据边界。
2. 定义官方零件与用户零件共用的版本化拼装输入契约，保留来源、审核和制造验证状态。
3. 先写跨包 fixture、坏数据、非法连接、保存刷新和越权测试，再接入拼装器。
4. 在真实浏览器完成零件选择/绘制 → 拼装 → 保存 → 刷新路径，并覆盖三个默认视口。
5. 通过后再接 Blockly、视觉仿真和部分导出，形成一条可重复的核心闭环 E2E。

并行 P1 仅做不改变产品规则的收敛：API 领域分层、Web 重复 DTO、`CodingPage`/字体体积，以及固定浏览器 E2E 基础设施。

## 7. 更新规则

只有出现新证据时才能推进状态。每次更新记录日期、验证对象或 commit、命令、自动化结果、人工路径、目标环境和未通过项；功能降级、回滚或证据失效时同步回退。
