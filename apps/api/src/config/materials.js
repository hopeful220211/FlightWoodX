// 材料 / 公差常量表（后台私有）。
// ⚠️ 永不进共享契约、永不下发给孩子：kerf/过盈/板厚是机器+材料属性，
// 与孩子的绘画交互完全隔离，前端和契约都看不到这张表。
//
// 本期（自制零件工坊 v1）只做 App 内挤出预览，不做制造导出，这张表先放着备用：
// - thicknessMm 是“给厚度”的事实来源（本期前端可硬编码同值，后端存档时核对一致）。
// - kerf/过盈/maxGap 等留给将来「可制造导出」（DXF/SVG，见后端文档 §5），本期不实现。
module.exports = {
  defaultMaterial: 'plywood-3mm',
  materials: {
    'plywood-3mm': {
      thicknessMm: 3,            // 真实板厚（待硬件组志豪/治远确认实测值）
      kerfMm: 0.15,              // 激光切口补偿（胶合板 0.1~0.3，切试条标定后冻结）
      pressFitClearanceMm: 0.1,  // 卡口过盈/间隙
      maxGapMm: 0.5,             // 总间隙硬上限
    },
    // 将来加第二种板就加一档；不开放给前端自由输入
  },
}
